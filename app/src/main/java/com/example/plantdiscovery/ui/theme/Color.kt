package com.example.plantdiscovery.ui.theme

import androidx.compose.ui.graphics.Color
val PrimaryGreen = Color(0xFF13D168)      // Vert vif pour boutons principaux (plus “Bloom”)
val PrimaryGreenDark = Color(0xFF089150)  // Vert foncé profond (hover, accents)
val PrimaryGreenLight = Color(0xFFE9F7EF) // Vert très clair pour backgrounds

val AccentGreen = Color(0xFF24E07D)       // Accent lumineux pour loader/FAB

val BackgroundLight = Color(0xFFF8F8F8)   // Fond général doux
val SurfaceWhite = Color(0xFFFFFFFF)      // Blanc pur pour cards/appbars

val TextPrimary = Color(0xFF212121)       // Texte principal
val TextSecondary = Color(0xFF757575)     // Texte secondaire
val TextHint = Color(0xFFB8B8B8)          // Texte hint dans les inputs

val ErrorRed = Color(0xFFF24D48)          // Rouge vif pour delete/warning

val OutlineLight = Color(0xFFF4F4F4)      // Pour les préviews ou séparateurs très soft

val NeutralGray = Color(0xFFE0E0E0)       // Gris clair pour les bords/inputs non actifs

